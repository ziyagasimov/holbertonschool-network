#!/usr/bin/env bash
# Configures an Ubuntu server with specific IP resolutions

# sed -i yerinə bu üsuldan istifadə edirik:
cat <<EOF > /etc/hosts
127.0.0.2 localhost
127.0.0.1 localhost
8.8.8.8 facebook.com
EOF
